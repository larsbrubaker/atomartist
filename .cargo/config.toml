[alias]
# Run the native demo and restart it whenever project files change.
# Requires cargo-watch (`cargo install cargo-watch`).
dev = ["watch", "-x", "run -p demo-native"]
# Take a screenshot of the running app and exit (CI / visual regression).
shot = "run -p demo-native -- --screenshot ./screenshot.png"
